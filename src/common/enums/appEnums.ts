export enum AccountRole {
    Admin = "admin",
    Landlord = "landlord",
    Tenant = "tenant",
}

export enum UserStatus {
    Active = "active",
    Inactive = "inactive",
}

export enum Gender {
    Male = "male",
    Female = "female",
    Other = "other",
}